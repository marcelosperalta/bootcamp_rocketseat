<!DOCTYPE html>

    <%- include("../partials/head"); %>

    <body class="container">
        <!-- <%- include("../partials/header", {page: "Home"}); %>  -->
        <%- include("../partials/header"); %> 

        <main>
            <div class="jumbotron">
                <h1>Node with EJS</h1>
                <p>We will learn how to work with EJS files</p>
                <p><%= subtitle %></p>
            </div>
            <ul>
                <% words.forEach(function(word) { %>
                    <li>
                        <strong><%= word.title %></strong><%= word.message %>
                    </li>
                <% }) %> 
            </ul>
        </main>

        <%- include("../partials/footer"); %> 
    </body>
</html>